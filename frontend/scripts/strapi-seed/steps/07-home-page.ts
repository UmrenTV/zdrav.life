/**
 * Home-page seed step. Featured relations have been removed; sections now
 * auto-fetch latest items based on latestCount / featuredOnly settings.
 * This step is kept as a placeholder for any future home-page seeding needs.
 */

export async function seedHomePage(): Promise<void> {
  console.log('  Home page uses automatic latest-content sections — no relations to seed.');
}
